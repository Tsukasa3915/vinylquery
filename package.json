{
  "name": "vinylquery",
  "version": "1.0.0",
  "description": "VinylQuery - Analog Record Search App",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

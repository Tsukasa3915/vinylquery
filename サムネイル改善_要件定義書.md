# サムネイル改善 要件定義書

## 1. 目的と背景
現在の検索結果に表示されるアルバムカバーのサムネイルにおいて、「正しくない（関係ない）画像が表示される」「画質が粗い」という2点の課題が発生していました。
これを解決するため、Discogs API のサムネイルをフォールバックとして残しつつ、より正確で高画質な公式ジャケット画像を取得できる別のAPIを導入することになりました。

## 2. 要件
1. **正確な画像の取得**:
   - ユーザーの検索意図（アーティスト名・タイトル）に合致する正確なアルバムカバーを取得すること。
2. **高画質化**:
   - 現在の `150x150` ピクセル等の粗い画像から、最低でも `600x600` ピクセル以上の高画質な画像を優先して取得・表示すること。
3. **別APIの活用**:
   - レートリミットが厳しく画質の低い Discogs API の画像取得処理に代わり、代替となる外部API（iTunes Search API）を利用すること。
4. **パフォーマンスへの配慮**:
   - 大量の検索結果に対して一つずつ直列で外部APIを叩くとレスポンスが遅延するため、上位20件程度を対象に非同期かつチャンク単位で画像取得処理を行うこと。

## 3. 実装方針
1. **iTunes Search API の導入**:
   - 認証不要かつ高品質なアートワークが取得可能な iTunes Search API をバックエンドに組み込みます。
   - エンドポイント: `https://itunes.apple.com/search?term=[Artist+Title]&entity=album&limit=1`
2. **高画質URLへの変換**:
   - APIから返却される画像URL (`100x100bb.jpg`) を `600x600bb.jpg` に置換することで、より高画質なアートワークを取得します。
3. **既存処理のクリーンアップ**:
   - 既存の `server/services/discogs.js` に存在した `enrichReleasesWithImages` メソッドは使用不可とし、削除します。
4. **ルーティングへの統合**:
   - `server/routes/search.js` のアーティスト検索 (`/artist`) およびフリーワード検索 (`/freeword`) の両ルートにて、レスポンス返却直前のリリース上位20件に対して画像差し替え処理を実行します。

## 4. 期待される効果
- UIの視覚的品質（Aesthetics）の大幅な向上。
- 正確なアルバムアートワークが表示されることによる、ユーザーのレコード検索体験の改善。
- （バックエンドで処理を完了させることで）フロントエンドにおけるチラつき（レイアウトシフト）の防止。

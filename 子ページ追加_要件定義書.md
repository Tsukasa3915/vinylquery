# 子ページ追加およびUI改善 要件定義書

## 1. 目的と背景
既存のシステムでは、検索結果一覧から作品をクリックすると外部サイト（Discogs）に直接遷移する仕様となっていました。
これに対し、「アプリ内で作品の詳細を確認できる専用の子ページ（詳細ページ）が欲しい」という要望があり、さらに「レコードが回るアニメーションのような遊び心のあるUIをもっと増やしてほしい」というご意見をいただきました。
本対応では、React Router を用いたページ遷移の実装と、レコード盤をモチーフにしたリッチなアニメーションの追加を行います。

## 2. 機能要件

### 2.1 ルーティングの導入と詳細ページ
- `react-router-dom` を導入し、シングルページアプリケーション（SPA）としてのページ遷移を実現する。
- 検索画面（ルート `/`）と、作品詳細画面（`/release/:id`）の2つのページを設ける。
- 検索結果の作品カード（ResultCard）をクリックした際、外部サイトではなく詳細画面へ遷移させる。

### 2.2 作品詳細ページ（ReleaseDetail）の表示項目
バックエンド API を経由して取得した情報を表示する。
- タイトル、アーティスト、リリース年、レーベル名、ジャンル等の基本情報
- アルバムの公式ジャケット画像
- 各トラック（収録曲）のポジション、タイトル、長さ（duration）のリスト

### 2.3 バックエンドの拡張
- `GET /api/search/release/:id` エンドポイントを新設する。
- フロントエンドからのリクエストパラメータ (`type=master` または `type=release`) を判別し、Discogs API の適切なエンドポイントを叩いて詳細情報を返却する。

## 3. 非機能・UI/UX要件

### 3.1 アニメーション（Aestheticsの向上）
- **一覧画面（ResultCard）**:
  - ホバー時にカード全体が少し浮き上がり、ジャケット画像の右側から黒いレコード盤がスライドして飛び出す。
  - 飛び出したレコード盤は回転し、ユーザーのクリックを促すインタラクティブなUIとする。
- **詳細画面（ReleaseDetail）**:
  - 画面上部（ヒーローセクション）に巨大なレコードプレイヤー風のUIを配置。
  - レコードスリーブ（ジャケット）と、その横でゆっくりと回転し続けるレコード盤を表示する。

### 3.2 UIのクリーンアップ
- 一覧画面の「年」の横にあった不要なカレンダーアイコン（📅）を削除し、スッキリとしたモダンなテキスト表記に変更する。

## 4. 期待される効果
- 外部サイトへの離脱を防ぎ、ユーザーがアプリ内でレコードを探索する体験（没入感）を向上させる。
- ダイナミックなレコード回転アニメーションにより、VinylQuery ブランドの「アナログレコード特有の良さ・楽しさ」を視覚的に表現する。
